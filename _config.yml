theme: octodoc

title: True Islam
description: A project to rectify all these issues about Islam
baseurl: "/" # the subpath of your site, e.g. /blog
url: "https://islam.octabyte.io" # the base hostname & protocol for your site, e.g. http://example.com

permalink: pretty
exclude: ["node_modules/", "*.gemspec", "*.gem", "Gemfile", "Gemfile.lock", "package.json", "package-lock.json",  "script/", "LICENSE.txt", "lib/", "bin/", "README.md", "Rakefile"]

# Enable or disable the site search
search_enabled: true

# Aux links for the upper right navigation
aux_links:
  "True Islam on GitHub":
    - "//github.com/OctaBytes/islam.github.io"

# Color scheme currently only supports "dark" or nil (default)
color_scheme: nil
